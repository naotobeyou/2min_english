<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>ダッシュボード | 2min English</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  <div class="card text-center" style="max-width: 500px; margin: 2rem auto;">
    <h1 class="text-lg">ようこそ、<%= user.username %> さん！</h1>
    <img src="/uploads/<%= user.avatar %>" alt="プロフィール画像" class="avatar" style="margin: 1rem auto;">
    
    <p><strong>英語レベル:</strong> <%= user.level %></p>
    <p><strong>趣味:</strong> <%= user.hobbies %></p>
    <p><strong>学習目的:</strong> <%= user.purpose %></p>

    <a href="/history" class="btn btn-secondary" style="margin-top: 1rem;">📜 メモ履歴を確認する</a>
    <a href="/edit-profile" class="btn btn-secondary" style="margin-top: 0.5rem;">✏️ プロフィールを編集</a>
    <a href="/settings" class="btn btn-secondary" style="margin-top: 0.5rem;">🎛️ 設定</a>

    <form action="/matching-wait" method="GET" style="margin-top: 1rem;">
      <button type="submit" class="btn btn-primary">🚀 マッチング開始</button>
    </form>

    <a href="/logout" class="btn btn-secondary" style="margin-top: 1rem;">ログアウト</a>
  </div>

  <script>
    const urlParams = new URLSearchParams(window.location.search);
    if (urlParams.get('blocked') === '1') {
      alert('✅ ユーザーをブロックしました');
    }
  </script>

</body>
</html>
